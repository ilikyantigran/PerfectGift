<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>PerfectGift</string>
    <key>CFBundleDisplayName</key>
    <string>PerfectGift</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>UILaunchScreen</key>
    <dict/>
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
    </dict>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
    </array>

    <!-- Gateway base URL: overrideable per environment; defaults to the local Docker stack.
         AppConfig reads PG_BASE_URL from the environment; this key is the built-in default. -->
    <key>PGBaseURL</key>
    <string>http://localhost:8080</string>
    <key>PGUniversalLinkHost</key>
    <string>perfectgift.app</string>

    <!-- Sign in with Apple / Google client identifiers, filled in at integration time. -->
    <key>PGAppleClientID</key>
    <string></string>
    <key>PGGoogleClientID</key>
    <string></string>

    <!-- Allow cleartext to localhost for the local Docker stack during development. -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsLocalNetworking</key>
        <true/>
    </dict>

    <!-- APNs: enable background remote notifications for "ideas ready" pushes. -->
    <key>UIBackgroundModes</key>
    <array>
        <string>remote-notification</string>
    </array>
</dict>
</plist>
